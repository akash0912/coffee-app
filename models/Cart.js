class Cart{
    constructor(id, quantity, name, price, sum){
        this.id = id,
        this.quantity =quantity,
        this.name = name,
        this.price = price,
        this.sum = sum
    }
}

export default Cart;